import styled from '@emotion/native';

import Colors from '@styles/colors';

export const bannerCOVID = styled.Text`
  background: ${Colors.tintColor};
  padding: 5px;
  text-align: center;
  width: 346px;
`;

export const textCOVID = styled.Text`
  flex: 1;
  font-family: Heebo;
  font-style: normal;
  font-weight: normal;
  font-size: 12px;
  line-height: 22px;
  color: #fff;
`;
